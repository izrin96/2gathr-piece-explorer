# Passwords for the self-hosted Postgres containers in docker-compose.prod.yml.
# Copy to .env.prod and fill in real values — docker-compose.prod.yml builds
# DATABASE_URL/INDEXER_DATABASE_URL for the website container from these.
DB_PASSWORD=changeme
INDEXER_DB_PASSWORD=changeme
